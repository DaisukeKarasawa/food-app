# 在庫管理機能の実装

## 概要

現在の食材管理システムでは、食材の有無のみを管理しており、実際の在庫数量を管理する機能がありません。実用的な食材管理システムにするために、在庫数量の管理機能を追加する必要があります。

## 実装する理由

1. **実用性の向上**: 食材の「ある/ない」だけでなく、「どのくらいあるか」を管理できることで、実際の家庭での食材管理により適したシステムになります。

2. **計画的な買い物**: 在庫数量を把握することで、必要な分だけ購入でき、食材の無駄や重複購入を防げます。

3. **料理の可否判定**: 料理に必要な食材の必要量と在庫量を比較して、実際に料理が作れるかを判定できます。

## 目的

- 食材の在庫数量を管理する機能を実装する
- 食材の消費による在庫減少を記録する機能を実装する
- 在庫数量に基づいた料理の可否判定機能を実装する
- 在庫が少なくなった食材を検出する機能を実装する

## 実装タスク

### 1. データベース設計の変更
- [ ] `foods`テーブルに`quantity`カラムを追加
- [ ] `foods`テーブルに`unit`カラムを追加（個、g、ml等の単位）
- [ ] `dishes_foods`テーブルに`required_quantity`カラムを追加（料理に必要な食材の量）
- [ ] マイグレーションファイルの作成

### 2. モデルの更新
- [ ] `Food`モデルに在庫数量のバリデーションを追加
- [ ] `Food`モデルに在庫不足を検出するスコープを追加
- [ ] `DishesFoods`モデルに必要量のバリデーションを追加
- [ ] 在庫消費を記録するメソッドを追加

### 3. GraphQL APIの拡張

#### Query
- [ ] 食材一覧取得時に在庫数量と単位を含める
- [ ] 在庫不足の食材を取得するクエリを追加
- [ ] 料理の可否判定を行うクエリを追加

#### Mutation
- [ ] 食材登録時に在庫数量と単位を設定
- [ ] 在庫数量を更新するミューテーションを追加
- [ ] 食材消費を記録するミューテーションを追加
- [ ] 料理作成時に必要な食材の量を消費するミューテーションを追加

### 4. ビジネスロジックの実装
- [ ] 在庫消費処理のサービスクラスを作成
- [ ] 料理可否判定のサービスクラスを作成
- [ ] 在庫不足検出のサービスクラスを作成

### 5. テストの実装
- [ ] 在庫管理機能のモデルテストを作成
- [ ] 在庫管理機能のGraphQLテストを作成
- [ ] 在庫消費処理のテストを作成
- [ ] 料理可否判定のテストを作成

### 6. ドキュメントの更新
- [ ] README.mdに在庫管理機能の説明を追加
- [ ] GraphQLスキーマのドキュメントを更新
- [ ] API使用例を追加

## 実装例

### 新しいフィールド
```ruby
# Food model
class Food < ApplicationRecord
  validates :quantity, presence: true, numericality: { greater_than_or_equal_to: 0 }
  validates :unit, presence: true
  
  scope :low_stock, -> { where('quantity < ?', 5) } # 在庫が少ない食材
  scope :in_stock, -> { where('quantity > 0') } # 在庫がある食材
end

# DishesFoods model
class DishesFood < ApplicationRecord
  validates :required_quantity, presence: true, numericality: { greater_than: 0 }
end
```

### 新しいクエリ例
```graphql
# 在庫不足の食材を取得
query {
  lowStockFoods {
    name
    quantity
    unit
  }
}

# 料理の可否判定
query {
  canMakeDish(name: "カレー") {
    canMake
    missingIngredients {
      name
      required
      available
    }
  }
}
```

### 新しいミューテーション例
```graphql
# 在庫数量を更新
mutation {
  updateFoodQuantity(input: {
    name: "じゃがいも"
    quantity: 10
  }) {
    food {
      name
      quantity
      unit
    }
  }
}

# 料理を作って在庫を消費
mutation {
  consumeIngredientsForDish(input: {
    dishName: "カレー"
    servings: 2
  }) {
    success
    consumedIngredients {
      name
      consumedQuantity
      remainingQuantity
    }
  }
}
```

## 実装の優先度

1. **高**: データベース設計変更、基本的な在庫数量管理
2. **中**: 在庫消費処理、料理可否判定
3. **低**: 在庫不足検出、レポート機能

## 想定される影響

- 既存のデータベースに新しいカラムを追加するためのマイグレーションが必要
- 既存のGraphQL APIに新しいフィールドが追加される（破壊的変更なし）
- 新しいビジネスロジックによる処理時間の増加は最小限

## 完了条件

- [ ] 食材の在庫数量が正常に管理できる
- [ ] 料理作成時に必要な食材の在庫が正常に消費される
- [ ] 在庫不足の食材を検出できる
- [ ] 料理の可否判定が正常に動作する
- [ ] 全ての新機能にテストが実装されている
- [ ] ドキュメントが更新されている

## 関連する既存の改善点

このIssueは、既存のREADMEに記載されている以下の改善点とも関連しています：
- テストの実装（在庫管理機能のテストも含む）
- エラーハンドリングの改善（在庫不足エラーなど）
- 価格設定の見直し（在庫数量と価格の関係）

## 補足

この機能の実装により、食材管理システムはより実用的になり、実際の家庭での食材管理により適したものになります。また、将来的にはフロントエンドとの連携時に、より豊富なユーザー体験を提供できるようになります。